extends Area2D

# Options are either "BLACK" or "WHITE" (all caps)
@export var exit_colour : String = "BLACK"




# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	pass
